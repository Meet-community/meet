name: telegram notifications
on:
  pull_request:
    types: [opened, closed, review_requested]
jobs:

  build:
    runs-on: ubuntu-latest
    name: Build
    steps:
      - name: send telegram message on action with pull request
        uses: appleboy/telegram-action@master

